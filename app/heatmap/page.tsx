import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Liquidity Heatmap',
  description: 'Real-time orderbook depth heatmap — Bookmap-style visualization of passive liquidity, trade flow, and market microstructure.',
};

export default function HeatmapPage() {
  return null;
}
