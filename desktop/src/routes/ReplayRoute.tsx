import { PlaceholderRoute } from "./PlaceholderRoute";

export function ReplayRoute() {
  return (
    <PlaceholderRoute
      icon="⏯"
      title="Session Replay"
      subtitle="Tick-by-tick replay of past sessions with pause / step / variable speed — backed by the existing /api/history snapshots."
      milestone="M9"
    />
  );
}
