import { PlaceholderRoute } from "./PlaceholderRoute";

export function GexRoute() {
  return (
    <PlaceholderRoute
      icon="⚡"
      title="GEX Dashboard"
      subtitle="Gamma exposure dashboard with strike-level gamma, dealer positioning, and a 3D GEX surface — driven by the existing /api/gex-* endpoints."
      milestone="M7"
    />
  );
}
