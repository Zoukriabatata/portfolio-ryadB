/**
 * INSTRUMENTATION - Next.js 15+
 * Sentry disabled — no auth token configured.
 * Re-enable by restoring sentry configs and uncommenting below.
 */

export async function register() {
  // Sentry disabled
}
